<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ua.kpi.comsys.iv7216">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileDevelopment6">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".films_list.AboutFilmActivity"/>
        <activity android:name=".films_list.AddFilmActivity"/>
        <activity android:name=".image_list.ImageActivity"/>
        <activity android:name=".films_list.FilmActivity"/>
        <activity android:name=".drawing.DrawingActivity"/>
    </application>

</manifest>